[workspace]
members = ["crates/ledger-core", "crates/ledger-sync", "src-tauri"]
default-members = ["crates/ledger-core", "crates/ledger-sync"]
resolver = "2"

[profile.release]
lto = "thin"
strip = true
